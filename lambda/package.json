{
  "name": "lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive ./test --EM_PROFILE test/test-profile.json",
    "test-watch": "mocha --watch --recursive ./test --EM_PROFILE test/test-profile.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "aws-sdk": "^2.26.0",
    "mocha": "^3.2.0",
    "rewire": "^2.5.2",
    "sinon": "^1.17.7"
  }
}
